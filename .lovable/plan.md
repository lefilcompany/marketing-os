Atualizar apenas as cores dos módulos em `src/lib/aeiou-modules.ts` para casar com a paleta pastel da referência (rosa/coral, amarelo/âmbar, azul, verde, rosa-lilás):

- **A — Ambiente**: `#E27D74` (coral/rosa avermelhado)
- **E — Estratégia**: `#E5B34B` (amarelo/âmbar)
- **I — Interações**: `#5FA8D3` (azul suave)
- **O — Operações**: `#6FB56A` (verde)
- **U — Unificação**: `#B87A9E` (rosa/mauve)

Apenas o campo `color` de cada `AeiouModule` (5 valores) será alterado. As cores individuais das ferramentas dentro de cada módulo permanecem como estão — não fazem parte da referência.

Sem outras mudanças.